# 2026-06-15 工作日志

## 阶段 1 + 2 重构实施

### 新增文件
- `src/face.rs` — 统一面方向定义模块（Face 枚举、FACES 常量、NEIGHBOR_OFFSETS、face_quad、face_quad_lod）
- `src/types.rs` — 核心类型定义模块（CHUNK_SIZE、CHUNK_VOLUME、BlockId、ChunkCoord、BlockPos）

### chunk.rs 变更
- 删除 Face 枚举、FACES 常量、FACE_UV_INDICES（移至 face.rs）
- 删除 ChunkCoord、BlockPos 定义（移至 types.rs）
- 删除 face_quad 函数（移至 face.rs）
- 删除 generate_chunk_mesh 函数（死代码，异步路径替代）
- 删除 spawn_chunk_entity 函数（死代码，异步路径替代）
- 删除 deprecated to_vec() 方法
- 删除 ChunkMeshHandle / ResourcePackManager / VoxelMaterial 导入（不再需要）
- 添加 re-export：`pub use crate::types::{CHUNK_SIZE, CHUNK_VOLUME, BlockId, ChunkCoord, BlockPos}`
- 添加 re-export：`pub use crate::face::{Face, FACES}`

### async_mesh.rs 变更
- 删除 FaceAsync 枚举和 FACES_ASYNC 常量（改用 face::Face / face::FACES）
- 删除 face_quad_async 函数（改用 face::face_quad）
- 删除 is_face_visible_async 函数（死代码，is_face_visible_fast 替代）

### lod.rs 变更
- 删除 FaceLod 枚举和 FACES_LOD 常量（改用 face::Face / face::FACES）
- 删除 face_quad_lod 函数（移至 face.rs）
- 删除 LodLevel::min_threshold()（死代码）
- 删除 LodManager::update()（死代码，update_incremental 替代）
- 删除 LodManager::chunk_distance()（死代码，chunk_distance_sq 替代）

### chunk_manager.rs 变更
- 删除本地 NEIGHBOR_OFFSETS 常量（改用 face::NEIGHBOR_OFFSETS）

### chunk_dirty.rs 变更
- 删除本地 NEIGHBOR_OFFSETS 常量（改用 face::NEIGHBOR_OFFSETS）

### compact_vertex.rs 变更
- 修复 normal_direction() bug：法线方向编码从 2 bit 扩展到 3 bit
  - 原始：bits 30-31 (2 bit) 只能表示 4 个方向，Front/Back 丢失
  - 修复：bits 29-31 (3 bit) 可表示全部 6 个方向
  - z 坐标从 10 bit 缩减到 9 bit (0-511)，仍远超 CHUNK_SIZE=32
- 更新文档注释以反映正确的位布局
- 新增 Front/Back 方向的编码/解码测试

### main.rs 变更
- 添加 `mod face;`、`mod types;`、`mod terrain_gen;`

### 阶段 3：模块拆分 + 性能优化

### terrain_gen.rs（新增）
- 从 chunk.rs 提取 fill_terrain / fill_flat_terrain / fill_menger_sponge / get_surface_height
- chunk.rs 从 ~910 行缩减到 ~445 行

### block_interaction.rs 变更
- boundary_neighbor_coords 改为 for_boundary_neighbors（闭包回调，零分配）

### chunk_manager.rs 变更
- collect_neighbors 改为 pub，供 chunk_dirty.rs 导入

### chunk_dirty.rs 变更
- 删除重复的 collect_neighbors，改为从 chunk_manager 导入

### chunk.rs 变更
- 洞穴缓存 HashMap 从每列创建改为全局复用（每列 clear()，零分配）

### Bug 修复
- terrain_gen.rs 导入修正：CHUNK_SIZE / ChunkCoord 从 `crate::terrain_noise` 改为 `crate::types`

### 阶段 4：常量统一 + 死代码清理

### 方块 ID 常量统一
- 将 AIR/GRASS/STONE/DIRT/SAND/WATER/TREE_TRUNK/TREE_LEAVES/SANDSTONE/SNOW_GRASS/GRAVEL/ROCK/MUD 从 biome.rs 移至 types.rs
- biome.rs 改为 `pub use crate::types::{...}` re-export
- chunk.rs re-export AIR/GRASS/STONE/DIRT/SAND/WATER
- 替换 20+ 处硬编码魔法数字：terrain_gen.rs、async_mesh.rs、chunk.rs、lod.rs、block_interaction.rs、block_definition.rs、chunk_dirty.rs

### 死代码清理
- 删除 ChunkData::flatten()（私有方法，从未被调用）
- 删除 fps_overlay.rs（孤立文件，未在 main.rs 注册，FPS 功能已合并到 hud.rs）
- 删除 async_mesh.rs 中未使用的 Face 导入
