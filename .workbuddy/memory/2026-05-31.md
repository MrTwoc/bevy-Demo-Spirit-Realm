# 2026-05-31

## 摄像机加速
- Shift 垂直下降改为 Ctrl 加速（3x），修改 `src/camera.rs` 的 `camera_movement` 函数
- 恢复 Shift 垂直下降逻辑，Shift=下降、Ctrl=3x 加速并存

## HUD 按键提示
- 左上角 HUD 末尾添加 8 行按键提示：[W/A/S/D] Move、[Space] Up、[Shift] Down、[Ctrl] Speed x3、[V] Wireframe Toggle、[ESC] Toggle Cursor、[Left-Click] Destroy、[Right-Click] Place
- 新增 `KeyBindingText` 标记组件，灰色 12px 字体，静态无需更新系统
