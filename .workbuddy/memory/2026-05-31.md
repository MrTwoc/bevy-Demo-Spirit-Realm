# 2026-05-31

## 摄像机加速
- Shift 垂直下降改为 Ctrl 加速（3x），修改 `src/camera.rs` 的 `camera_movement` 函数
- 恢复 Shift 垂直下降逻辑，Shift=下降、Ctrl=3x 加速并存

## HUD 按键提示
- 左上角 HUD 末尾添加 8 行按键提示：[W/A/S/D] Move、[Space] Up、[Shift] Down、[Ctrl] Speed x3、[V] Wireframe Toggle、[ESC] Toggle Cursor、[Left-Click] Destroy、[Right-Click] Place
- 新增 `KeyBindingText` 标记组件，灰色 12px 字体，静态无需更新系统

## P0 性能优化（已落地 3/4）
- **P0-1 LOD 去 sqrt**: `lod.rs` 新增 `chunk_distance_sq` + `should_switch_sq`，`update` 和 `update_incremental` 改用平方距离，省 200 次 sqrt/帧
- **P0-2 LRU 淘汰优化**: `chunk_manager.rs` 的 `lru_evict` 用 HashSet 收集淘汰坐标 + `retain` 批量过滤，替代 O(N×evict_count) 的逐条 position+swap_remove
- **P0-3 合并 HashMap 查找**: `collect_and_upload_meshes` 从 6 次 HashMap lookup/区块降到 2 次（1 get + 1 get_mut）
- **P0-4 加载队列截断**: **已回滚**。截断 128 远不够视距 16 的 ~5400 区块，导致初始加载视距仅约 8
