使用方式

  加新方块只需两步（无需改代码）：
  1. 在 assets/blockstates/ 下创建 JSON 文件
  2. 在材质包目录下放入对应 PNG 纹理
---  

  1. 加纹理 PNG — 放到材质包目录下

  assets/resourcepacks/1号材质包/emerald_block.png

  2. 加方块定义 JSON — 放到 blockstates 目录下

  // assets/blockstates/emerald_block.json
  
---

  后续计划：
  Minecraft 的演进历史：

  ┌──────┬──────────────────────────────┬──────────────────────────────────────┐
  │ 版本 │             方案             │                 问题                 │
  ├──────┼──────────────────────────────┼──────────────────────────────────────┤
  │ 早年 │ 数字 ID 写死（0-255）        │ 模组冲突，两个模组都用 ID 100 就炸了 │
  ├──────┼──────────────────────────────┼──────────────────────────────────────┤
  │ 现在 │ 字符串 ID（minecraft:stone） │ 不冲突，数字 ID 运行时自动分配       │
  └──────┴──────────────────────────────┴──────────────────────────────────────┘

  我们的 JSON 也可以改成字符串 ID 方案：

  // 方块定义用字符串 ID
  { "id": "emerald_block", "solid": true, "textures": {...} }

  // 运行时自动分配数字 ID
  // 0 → air
  // 1 → grass_block
  // 2 → stone
  // ...
  // 13 → emerald_block（自动分配，不写死）

  数字 ID 只在 chunk 数据存储时使用（省空间），玩家/模组作者只接触字符串 ID。

  但现阶段不建议改，原因：
  - 方块数量 < 50，冲突概率极低
  - 字符串 ID 会增加 chunk 存储复杂度（需要一个 string↔u8 的注册表）
  - 当前重点是功能完善，不是模组架构

  等真正要做模组系统时再改，改动量不大——主要是把 JSON 里的 "id": 13 换成 "id": "emerald_block"，然后加一个运行时 ID
  注册器。